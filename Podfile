# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwitchIM' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  pod 'MASShortcut', '~> 2.3'
  use_frameworks!

  # Pods for SwitchIM

  target 'SwitchIMTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
